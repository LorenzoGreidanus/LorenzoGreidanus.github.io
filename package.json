{
  "name": "meneergreidanus",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --persist-to ../.wrangler-state",
    "deploy": "wrangler deploy",
    "toets": "node server/audit-bank.js && node server/audit-donker.js"
  },
  "devDependencies": {
    "wrangler": "^4.131.1"
  }
}
