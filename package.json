{
  "name": "meneergreidanus",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --persist-to ../.wrangler-state",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.131.1"
  }
}
