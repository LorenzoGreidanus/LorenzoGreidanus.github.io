<!DOCTYPE html>
<html lang="nl">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Lorenzo Greidanus &middot; Docent geschiedenis</title>
<meta name="description" content="Persoonlijke site van Lorenzo Greidanus, docent geschiedenis met een digitale inslag. Werkvormen, materiaal en contact.">
<meta name="theme-color" content="#204ECF">

<!-- Open graph, handig als je de link deelt -->
<meta property="og:type" content="website">
<meta property="og:title" content="Lorenzo Greidanus &middot; Docent geschiedenis">
<meta property="og:description" content="Geschiedenis die leeft. Werkvormen, materiaal en contact.">

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Caveat:wght@500;600&family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">

<link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><rect width='100' height='100' rx='26' fill='%23204ECF'/><text x='50' y='68' font-family='Poppins,Arial' font-size='46' font-weight='600' fill='%23FBF6F1' text-anchor='middle'>LG</text></svg>">

<style>
/* ============================================================
   MERKKLEUREN, uit het merkboek
   ============================================================ */
:root{
  --crab:#F26749;
  --amber:#EA9836;
  --ocean:#204ECF;
  --vista:#83A5F2;
  --sand:#FCDED6;
  --room:#FBF6F1;

  --ink:#14224C;
  --muted:#5b6480;
  --line:#e7ddd6;

  --r-s:14px;
  --r-m:20px;
  --r-l:28px;
  --shadow:0 10px 0 rgba(20,34,76,.08);
  --shadow-soft:0 14px 34px rgba(20,34,76,.10);
  --max:1080px;
}

*{box-sizing:border-box}
html{scroll-behavior:smooth}
body{
  margin:0;
  background:var(--room);
  color:var(--ink);
  font-family:'Poppins',system-ui,-apple-system,'Segoe UI',Arial,sans-serif;
  font-weight:400;
  line-height:1.65;
  -webkit-font-smoothing:antialiased;
}
img{max-width:100%;display:block}
a{color:inherit}

.wrap{width:min(100% - 40px, var(--max));margin-inline:auto}

h1,h2,h3{font-weight:600;line-height:1.15;margin:0}
h2{font-size:clamp(1.7rem,4vw,2.6rem);letter-spacing:-.01em}
h3{font-size:1.15rem}
p{margin:0 0 1rem}

.hand{font-family:'Caveat',cursive;font-weight:600;font-size:1.6rem;line-height:1;color:var(--crab)}
.label{
  display:inline-block;font-size:.72rem;font-weight:600;letter-spacing:.16em;
  text-transform:uppercase;color:var(--muted);margin-bottom:.5rem;
}

/* ============================================================
   NAVIGATIE
   ============================================================ */
.skip{
  position:absolute;left:-9999px;top:0;background:var(--ink);color:#fff;
  padding:10px 16px;border-radius:0 0 var(--r-s) 0;z-index:99;
}
.skip:focus{left:0}

header.nav{position:sticky;top:0;z-index:40;padding:14px 0;
  background:rgba(251,246,241,.86);backdrop-filter:blur(8px);border-bottom:1px solid transparent}
header.nav.scrolled{border-bottom-color:var(--line)}
.nav-in{display:flex;align-items:center;justify-content:space-between;gap:16px}
.mark{display:flex;align-items:center;gap:10px;text-decoration:none;font-weight:600}
.mark span.badge{
  width:36px;height:36px;border-radius:11px;background:var(--ocean);color:var(--room);
  display:flex;align-items:center;justify-content:center;font-size:.85rem;letter-spacing:.02em;
}
.nav-links{display:flex;gap:6px;align-items:center}
.nav-links a{
  text-decoration:none;font-size:.92rem;font-weight:500;color:var(--ink);
  padding:8px 14px;border-radius:999px;transition:background .18s ease,color .18s ease;
}
.nav-links a:hover{background:var(--sand)}
.nav-links a.cta{background:var(--ocean);color:var(--room)}
.nav-links a.cta:hover{background:#1a41ae}
@media(max-width:720px){.nav-links a:not(.cta){display:none}}

/* ============================================================
   HERO
   ============================================================ */
.hero{position:relative;overflow:hidden;padding:72px 0 96px;background:var(--ocean);color:var(--room)}
.hero::after{
  content:"";position:absolute;left:0;right:0;bottom:-1px;height:70px;background:var(--room);
  border-radius:60% 40% 0 0 / 100% 100% 0 0;
}
.hero-in{position:relative;z-index:2;display:grid;grid-template-columns:1.15fr .85fr;gap:48px;align-items:center}
@media(max-width:860px){.hero-in{grid-template-columns:1fr;gap:36px}.hero{padding-bottom:80px}}

.hero .hand{color:var(--sand)}
.hero h1{font-size:clamp(2.4rem,6.2vw,4rem);margin:.35rem 0 1rem;letter-spacing:-.02em}
.hero p.lead{font-size:1.08rem;color:#dfe7ff;max-width:46ch}
.hero-buttons{display:flex;flex-wrap:wrap;gap:12px;margin-top:26px}

.btn{
  display:inline-block;text-decoration:none;font-weight:600;font-size:.95rem;
  padding:13px 24px;border-radius:999px;box-shadow:var(--shadow);
  transition:transform .16s ease,box-shadow .16s ease;
}
.btn:hover{transform:translateY(-2px);box-shadow:0 12px 0 rgba(20,34,76,.12)}
.btn:active{transform:translateY(1px);box-shadow:0 6px 0 rgba(20,34,76,.10)}
.btn-crab{background:var(--crab);color:#fff}
.btn-ghost{background:transparent;color:var(--room);box-shadow:none;border:1.5px solid rgba(251,246,241,.45)}
.btn-ghost:hover{background:rgba(251,246,241,.12);box-shadow:none}
.btn-ocean{background:var(--ocean);color:var(--room)}

/* blob met foto */
.portrait{position:relative;justify-self:center;width:min(320px,80vw);aspect-ratio:1;}
.portrait .blob{
  width:100%;height:100%;overflow:hidden;background:var(--vista);
  border-radius:58% 42% 46% 54% / 48% 52% 48% 52%;
  display:flex;align-items:center;justify-content:center;
  box-shadow:0 18px 40px rgba(6,20,60,.28);
}
.portrait .blob img{width:100%;height:100%;object-fit:cover}
.portrait .initials{font-size:4.4rem;font-weight:600;color:var(--room);letter-spacing:.02em}

/* stickerchips, zwevende bubbels */
.bub{
  position:absolute;font-size:.82rem;font-weight:600;padding:8px 15px;border-radius:999px;
  box-shadow:0 8px 18px rgba(6,20,60,.20);white-space:nowrap;
}
.bub.a{background:var(--crab);color:#fff;top:2%;right:-6%;transform:rotate(-6deg)}
.bub.b{background:var(--amber);color:var(--ink);bottom:16%;left:-12%;transform:rotate(5deg)}
.bub.c{background:var(--sand);color:var(--ink);bottom:-2%;right:4%;transform:rotate(-3deg)}
@media(prefers-reduced-motion:no-preference){
  .bub.a{animation:float 7s ease-in-out infinite}
  .bub.b{animation:float 8.5s ease-in-out infinite .6s}
  .bub.c{animation:float 7.8s ease-in-out infinite 1.2s}
}
@keyframes float{0%,100%{translate:0 0}50%{translate:0 -9px}}

/* ============================================================
   SECTIES
   ============================================================ */
section{padding:72px 0}
.sec-head{max-width:60ch;margin-bottom:38px}
.sec-head p{color:var(--muted)}

/* Over mij */
.about{display:grid;grid-template-columns:1.25fr .75fr;gap:46px;align-items:start}
@media(max-width:860px){.about{grid-template-columns:1fr}}
.about p{color:#3a4364}
.chips{display:flex;flex-wrap:wrap;gap:10px;margin-top:8px}
.chip{
  font-size:.85rem;font-weight:600;padding:8px 15px;border-radius:999px;
  box-shadow:0 6px 14px rgba(20,34,76,.08);
}
.chip:nth-child(4n+1){background:var(--sand);color:var(--ink);transform:rotate(-2deg)}
.chip:nth-child(4n+2){background:var(--vista);color:var(--ink);transform:rotate(1.5deg)}
.chip:nth-child(4n+3){background:var(--amber);color:var(--ink);transform:rotate(-1deg)}
.chip:nth-child(4n+4){background:var(--crab);color:#fff;transform:rotate(2deg)}

.facts{background:#fff;border:1px solid var(--line);border-radius:var(--r-l);padding:26px;box-shadow:var(--shadow-soft)}
.facts h3{margin-bottom:14px}
.facts ul{list-style:none;margin:0;padding:0}
.facts li{display:flex;gap:12px;padding:10px 0;border-top:1px solid var(--line);font-size:.94rem}
.facts li:first-child{border-top:0}
.facts li b{font-weight:600;min-width:96px;color:var(--muted);font-size:.8rem;letter-spacing:.06em;text-transform:uppercase;padding-top:3px}

/* Aanpak, kaarten */
.cards{display:grid;grid-template-columns:repeat(auto-fit,minmax(240px,1fr));gap:20px}
.card{
  background:#fff;border:1px solid var(--line);border-radius:var(--r-l);padding:26px;
  transition:transform .18s ease,box-shadow .18s ease;
}
.card:hover{transform:translateY(-4px);box-shadow:var(--shadow-soft)}
.card .tile{
  width:48px;height:48px;border-radius:16px 16px 16px 6px;
  display:flex;align-items:center;justify-content:center;margin-bottom:16px;
}
.card .tile svg{width:24px;height:24px;fill:none;stroke-width:1.9;stroke-linecap:round;stroke-linejoin:round}
.card p{margin:10px 0 0;color:var(--muted);font-size:.94rem}

/* Materiaal */
.materiaal{background:#fff;border-top:1px solid var(--line);border-bottom:1px solid var(--line)}
.empty{
  border:2px dashed var(--vista);border-radius:var(--r-l);padding:44px 26px;text-align:center;background:var(--room);
}
.empty .hand{display:block;margin-bottom:8px}
.empty p{color:var(--muted);max-width:44ch;margin-inline:auto}

/* Contact */
.contact-box{
  background:var(--ocean);color:var(--room);border-radius:var(--r-l);padding:clamp(30px,5vw,54px);
  display:grid;grid-template-columns:1.1fr .9fr;gap:34px;align-items:center;position:relative;overflow:hidden;
}
@media(max-width:760px){.contact-box{grid-template-columns:1fr}}
.contact-box::before{
  content:"";position:absolute;width:280px;height:280px;background:rgba(131,165,242,.28);
  border-radius:50%;right:-90px;top:-110px;
}
.contact-box h2{color:var(--room);position:relative}
.contact-box p{color:#dfe7ff;position:relative}
.contact-links{display:flex;flex-direction:column;gap:12px;position:relative}
.contact-links a{
  display:flex;align-items:center;gap:12px;text-decoration:none;background:rgba(251,246,241,.10);
  border:1px solid rgba(251,246,241,.22);border-radius:var(--r-m);padding:14px 18px;font-weight:500;
  transition:background .18s ease,transform .18s ease;
}
.contact-links a:hover{background:rgba(251,246,241,.20);transform:translateX(3px)}
.contact-links svg{width:20px;height:20px;stroke:currentColor;fill:none;stroke-width:1.8;stroke-linecap:round;stroke-linejoin:round;flex:none}

/* Footer */
footer{padding:34px 0 46px;font-size:.88rem;color:var(--muted)}
.foot-in{display:flex;flex-wrap:wrap;gap:14px;justify-content:space-between;align-items:center}
.strip{display:flex;height:8px;border-radius:999px;overflow:hidden;width:170px}
.strip i{flex:1}

/* Toegankelijkheid en beweging */
a:focus-visible,button:focus-visible{outline:3px solid var(--amber);outline-offset:3px;border-radius:6px}
.reveal{opacity:0;transform:translateY(18px);transition:opacity .55s ease,transform .55s ease}
.reveal.in{opacity:1;transform:none}
@media(prefers-reduced-motion:reduce){
  html{scroll-behavior:auto}
  .reveal{opacity:1;transform:none;transition:none}
  *{animation:none !important}
}
</style>
</head>
<body>

<a class="skip" href="#main">Naar de inhoud</a>

<header class="nav" id="nav">
  <div class="wrap nav-in">
    <a class="mark" href="#top"><span class="badge">LG</span> Lorenzo Greidanus</a>
    <nav class="nav-links" aria-label="Hoofdmenu">
      <a href="#over">Over mij</a>
      <a href="#aanpak">Mijn aanpak</a>
      <a href="#materiaal">Materiaal</a>
      <a class="cta" href="#contact">Contact</a>
    </nav>
  </div>
</header>

<main id="main">

<!-- ============================ HERO ============================ -->
<section class="hero" id="top">
  <div class="wrap hero-in">
    <div>
      <span class="hand">hoi, ik ben</span>
      <h1>Lorenzo Greidanus</h1>
      <p class="lead">Docent geschiedenis met een digitale inslag. Ik maak van het verleden iets waar leerlingen vandaag iets aan hebben, in een klas waar je fouten mag maken.</p>
      <div class="hero-buttons">
        <a class="btn btn-crab" href="#aanpak">Bekijk mijn werk</a>
        <a class="btn btn-ghost" href="#contact">Neem contact op</a>
      </div>
    </div>

    <div class="portrait">
      <div class="blob">
        <!-- Zet een foto naast index.html met de naam foto.jpg, dan verschijnt die hier vanzelf.
             Geen foto? Dan blijven de initialen staan. -->
        <img src="foto.jpg" alt="Portret van Lorenzo Greidanus" onerror="this.remove();document.getElementById('initialen').hidden=false">
        <span class="initials" id="initialen" hidden>LG</span>
      </div>
      <span class="bub a">Het nu</span>
      <span class="bub b">Gouden Eeuw</span>
      <span class="bub c">Bronnen lezen</span>
    </div>
  </div>
</section>

<!-- ============================ OVER MIJ ============================ -->
<section id="over">
  <div class="wrap about reveal">
    <div>
      <span class="label">over mij</span>
      <h2>Serieus vak, leuk leren</h2>
      <p style="margin-top:18px">Voordat ik voor het onderwijs koos, rondde ik de mbo-opleiding Game Developer af. Die makende, spelende manier van denken neem ik mee de klas in.</p>
      <p>Goed onderwijs begint voor mij bij de relatie met leerlingen. Pas in een veilig en voorspelbaar klimaat durven ze fouten te maken, vragen te stellen en zichzelf te zijn. Daar komt leren op gang.</p>
      <p>Geschiedenis is geen rij jaartallen, maar een manier om de wereld van nu beter te begrijpen. Ik werk evidence-informed, met Positive Behavior Support en formatief evalueren als basis, en houd mijn vak bij de tijd met digitale en mediawijze werkvormen.</p>
      <div class="chips">
        <span class="chip">Speels</span>
        <span class="chip">Warm</span>
        <span class="chip">Helder</span>
        <span class="chip">Vakkundig</span>
        <span class="chip">Uitnodigend</span>
      </div>
    </div>

    <aside class="facts">
      <h3>In het kort</h3>
      <ul>
        <li><b>Vak</b><span>Geschiedenis, mens en maatschappij</span></li>
        <li><b>Nu</b><span>Docent en mentor in het voortgezet speciaal onderwijs</span></li>
        <li><b>Opleiding</b><span>Tweedegraads lerarenopleiding Geschiedenis, Windesheim Zwolle</span></li>
        <li><b>Basis</b><span>EDI, formatief evalueren, Positive Behavior Support</span></li>
        <li><b>Plaats</b><span>Emmeloord, Noordoostpolder</span></li>
      </ul>
    </aside>
  </div>
</section>

<!-- ============================ AANPAK ============================ -->
<section id="aanpak" style="padding-top:0">
  <div class="wrap">
    <div class="sec-head reveal">
      <span class="label">mijn aanpak</span>
      <h2>Waar mijn lessen op rusten</h2>
      <p>Vier uitgangspunten die in elke les terugkomen, van een instructie van tien minuten tot een hele projectweek.</p>
    </div>

    <div class="cards reveal">
      <article class="card">
        <div class="tile" style="background:var(--ocean)">
          <svg viewBox="0 0 24 24" stroke="#FBF6F1"><path d="M3 6.5A2.5 2.5 0 0 1 5.5 4H11v16H5.5A2.5 2.5 0 0 1 3 17.5z"/><path d="M21 6.5A2.5 2.5 0 0 0 18.5 4H13v16h5.5a2.5 2.5 0 0 0 2.5-2.5z"/></svg>
        </div>
        <h3>Geschiedenis die leeft</h3>
        <p>Ik begin bij een vraag uit het nu en werk terug naar de bron. Zo krijgt het verleden een reden om er te zijn.</p>
      </article>

      <article class="card">
        <div class="tile" style="background:var(--crab)">
          <svg viewBox="0 0 24 24" stroke="#fff"><path d="M20 6 9 17l-5-5"/></svg>
        </div>
        <h3>Formatief evalueren</h3>
        <p>Feed up, feed back en feed forward. Leerlingen weten waar ze staan, wat de volgende stap is en hoe ze die zetten.</p>
      </article>

      <article class="card">
        <div class="tile" style="background:var(--amber)">
          <svg viewBox="0 0 24 24" stroke="#14224C"><rect x="2.5" y="4" width="19" height="13" rx="2"/><path d="M8 21h8M12 17v4"/></svg>
        </div>
        <h3>Digitaal en mediawijs</h3>
        <p>Van AI-tools tot bronnenonderzoek online. Techniek is een middel, nooit het doel van de les.</p>
      </article>

      <article class="card">
        <div class="tile" style="background:var(--vista)">
          <svg viewBox="0 0 24 24" stroke="#14224C"><circle cx="9" cy="8" r="3.2"/><path d="M2.5 20a6.5 6.5 0 0 1 13 0"/><path d="M16 5.5a3 3 0 0 1 0 5.4M18.2 20a6.4 6.4 0 0 0-2.3-4.9"/></svg>
        </div>
        <h3>Mentor en structuur</h3>
        <p>Vaste routines, duidelijke verwachtingen en persoonlijke aandacht. Rust in de klas geeft ruimte om te leren.</p>
      </article>
    </div>
  </div>
</section>

<!-- ============================ MATERIAAL ============================ -->
<section class="materiaal" id="materiaal">
  <div class="wrap">
    <div class="sec-head reveal">
      <span class="label">werkvormen en materiaal</span>
      <h2>Wat ik deel</h2>
      <p>Lesmateriaal, werkvormen en hulpmiddelen die ik zelf maak en gebruik. Vrij te gebruiken in je eigen klas.</p>
    </div>

    <!-- Zolang er nog niets staat, blijft dit blok zichtbaar.
         Zodra je hieronder kaarten toevoegt, kun je dit blok weghalen. -->
    <div class="empty reveal">
      <span class="hand">binnenkort meer</span>
      <p>Hier komen de eerste werkvormen en downloads te staan.</p>
    </div>

    <!-- ============================================================
         KAART TOEVOEGEN: kopieer het blok hieronder, plak het binnen
         <div class="cards"> ... </div> en pas de tekst en de link aan.

    <div class="cards">
      <a class="card" href="materiaal/naam-bestand.pdf" style="text-decoration:none">
        <div class="tile" style="background:var(--sand)">
          <svg viewBox="0 0 24 24" stroke="#14224C"><path d="M14 3H7a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2V8z"/><path d="M14 3v5h5"/></svg>
        </div>
        <h3>Naam van de werkvorm</h3>
        <p>Eén zin over wat het is en voor welke klas het werkt.</p>
      </a>
    </div>

         ============================================================ -->
  </div>
</section>

<!-- ============================ CONTACT ============================ -->
<section id="contact">
  <div class="wrap">
    <div class="contact-box reveal">
      <div>
        <span class="hand" style="color:var(--sand)">even kennismaken?</span>
        <h2 style="margin:.3rem 0 .8rem">Ik hoor graag van je</h2>
        <p style="margin:0">Vragen over een werkvorm, samenwerken aan lesmateriaal of een vacature. Stuur gerust een bericht.</p>
      </div>
      <div class="contact-links">
        <!-- Pas hier je eigen gegevens en links aan. -->
        <a href="mailto:lorenzogreidanus@gmail.com">
          <svg viewBox="0 0 24 24"><rect x="2.5" y="4.5" width="19" height="15" rx="2.5"/><path d="m3 7 9 6 9-6"/></svg>
          lorenzogreidanus@gmail.com
        </a>
        <a href="https://github.com/" rel="me">
          <svg viewBox="0 0 24 24"><path d="M9 19c-4.5 1.4-4.5-2.3-6-3m12 5v-3.6a3.1 3.1 0 0 0-.9-2.4c2.9-.3 6-1.4 6-6.4a4.9 4.9 0 0 0-1.4-3.4 4.5 4.5 0 0 0-.1-3.4s-1.1-.3-3.6 1.4a12.4 12.4 0 0 0-6.4 0C6.1 1.1 5 1.4 5 1.4a4.5 4.5 0 0 0-.1 3.4A4.9 4.9 0 0 0 3.5 8.3c0 4.9 3 6 5.9 6.4a3.1 3.1 0 0 0-.9 2.3V21"/></svg>
          GitHub
        </a>
        <a href="https://www.linkedin.com/">
          <svg viewBox="0 0 24 24"><rect x="3" y="3" width="18" height="18" rx="3"/><path d="M7.5 10.5V17M7.5 7.5v.01M11.5 17v-3.6a2.4 2.4 0 0 1 4.8 0V17"/></svg>
          LinkedIn
        </a>
      </div>
    </div>
  </div>
</section>

</main>

<footer>
  <div class="wrap foot-in">
    <div class="strip" aria-hidden="true">
      <i style="background:var(--crab)"></i><i style="background:var(--amber)"></i><i style="background:var(--ocean)"></i><i style="background:var(--vista)"></i><i style="background:var(--sand)"></i>
    </div>
    <span>&copy; <span id="jaar">2026</span> Lorenzo Greidanus &middot; Docent geschiedenis</span>
  </div>
</footer>

<script>
document.getElementById('jaar').textContent = new Date().getFullYear();

// Randlijn onder de navigatie zodra je scrollt
const nav = document.getElementById('nav');
const onScroll = () => nav.classList.toggle('scrolled', window.scrollY > 8);
onScroll();
addEventListener('scroll', onScroll, {passive:true});

// Secties rustig in beeld laten komen
const reduced = matchMedia('(prefers-reduced-motion: reduce)').matches;
const items = document.querySelectorAll('.reveal');
if (reduced || !('IntersectionObserver' in window)) {
  items.forEach(el => el.classList.add('in'));
} else {
  const io = new IntersectionObserver((entries) => {
    entries.forEach(e => { if (e.isIntersecting) { e.target.classList.add('in'); io.unobserve(e.target); } });
  }, {threshold:.12, rootMargin:'0px 0px -40px 0px'});
  items.forEach(el => io.observe(el));
}
</script>
</body>
</html>
